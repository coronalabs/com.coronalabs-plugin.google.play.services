Sources for the plugin `plugin.google.play.services`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.google.play.services" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
